[package]
name = "desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = [] }
ui = { workspace = true, features = ["desktop-web"] }
image = "0.25.8"

[features]
default = ["desktop"]
desktop = ["dioxus/desktop"]

